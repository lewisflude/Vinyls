<div class="selection-art">
  <%= image_tag @selection.album.album_art.url(:medium) %> 
</div>

<div class="selection-info">
  <div class='username-header'>Selected by <%= link_to @selection.user.username, @selection.user %></div>
  <ul>
    <li class="selection-artist"><%= @selection.album.artist %></li>
    <li class="selection-title"><%= @selection.album.title %></li>
    <% if @selection.description %>
      <li class="selection-description"><%= @selection.description %></li>
    <% end %>
  </ul>
</div>

<nav class="secondary">
  <ul>
    <% if can? :update, @selection %>
      <li><%= link_to "Edit", edit_selection_path %></li>
    <% end %>
    <% if can? :destroy, @selection %>
      <li><%= link_to 'Remove', { action: :destroy, id: @selection.id }, method: :delete, data: { confirm: 'Are you sure?' } %></li>
    <% end %>
      <li><%= link_to 'Back to List', root_path %></li>
  </ul>
</nav>



